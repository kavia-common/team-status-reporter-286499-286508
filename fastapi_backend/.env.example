# Backend environment configuration for Team Status Reporter

# Environment
ENV=development

# Database
# SQLAlchemy/PostgreSQL URL, e.g. postgresql+psycopg://postgres:password@localhost:5432/postgres
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/database

# JWT configuration
# IMPORTANT: Replace with a strong secret in production
JWT_SECRET=please-set-a-strong-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_MINUTES=43200

# Cookie settings for refresh token
# Set COOKIE_SECURE=true if served over HTTPS
COOKIE_SECURE=false
# Optional cookie domain (e.g., example.com); leave empty for default
COOKIE_DOMAIN=
# One of: lax, strict, none
COOKIE_SAMESITE=lax
REFRESH_TOKEN_COOKIE_NAME=refresh_token

# CORS
# Set to your Angular app origin to enable credentials/cookies
FRONTEND_ORIGIN=http://localhost:3000
# Additional allowed origins as comma-separated list
ADDITIONAL_CORS_ORIGINS=
# App metadata (optional)
APP_TITLE=Team Status Reporter API
APP_DESCRIPTION=API for authentication and weekly status reporting features.
APP_VERSION=0.1.0
